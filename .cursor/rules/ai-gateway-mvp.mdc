 ---
description: AI Gateway MVP 开发与表达规范（项目级）
alwaysApply: true
---

# AI Gateway MVP 规则

## 1) 表达规则（强制）

- 只用明确词，不用模糊词。
- 禁止：`可能`、`大概`、`应该`、`尽量`、`适当`、`酌情`、`建议考虑`。
- 必须写清：输入、处理步骤、输出、失败处理。
- 设计说明用短句，每句只表达一个意思。
- 提交代码时，先给“为什么这样做”，再给“改了什么”。

## 2) MVP 目标（强制）

第一阶段只实现并验证以下 4 步闭环：  

1. 识别问题
2. 调 OEM REST API 取监控数据
3. 查询知识库（先用单个运维文档）
4. 组织结构化回答并返回 CLI

不在 MVP 范围内的功能一律不做：自动修复、自动变更、复杂多轮工作流、Web 前端。

## 3) 数据与接口边界（强制）

- OEM 数据只通过 REST API 获取。
- 禁止直连 OEM repository 数据库。
- 禁止高风险写操作；第一阶段只读。
- 客户端入口固定为 CLI。

## 4) 问题识别规则（强制）

识别时必须同时抽取：

- 意图类型：`状态查询` | `告警汇总` | `单目标诊断` | `趋势分析` | `SOP问答` | `风险排序`
- 目标对象：主机名/数据库名/一体机对象
- 指标对象：如 `Memory_HardwareCorrupted`、`DiskErrorCount`
- 时间范围：默认 `24h`，用户指定时优先用用户输入

若缺少关键参数，必须返回明确追问，不得猜测补全。

## 5) 回答格式（强制）

每次回答固定为 4 段：

1. 结论
2. 证据（指标值、阈值、时间范围、相关事件/告警）
3. 下一步建议（最多 3 条，必须可执行）
4. 深挖入口（Grafana 链接或知识库来源）

禁止只返回泛化解释，不给证据。
